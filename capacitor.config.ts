import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-label-stacked-with-chip-bug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
